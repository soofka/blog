export const languages = {
  EN: 'en',
  PL: 'pl',
};

export const defaultLanguage = languages.EN;
export const languageParameter = 'lang';
//@TODO: remove, switch import to /common, languageParameter => LANGUAGE_QUERY_STRING_PARAMETER
